function addSomething(num, str){  // define a function with two parameters
	var add = num + str;          // declare and initialize a variable to store the sum of the two parameters
	
	alert(add);                  // display an alert box containing the add variable 
}

addSomething(455, " programs to write.");  // call the function to use it 